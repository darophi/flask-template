language: python
python: 
  - "3.6"

cache: pip

install: 
  # Install all the dependencies that are mentioned in the setup.py
  - pip install .

script: 
  # Find all the test automatically
  - python -m unittest discover --verbose