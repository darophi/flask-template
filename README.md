# Template Flask Rest API
The Rest API for the android application

## Install Virtual Environment

- `virtualenv --python=python3.6 venv`

## Install Dependencies
Run the following command inside the root of the directory to install all the dependencies:
- `pip install .`

